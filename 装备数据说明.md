# 装备数据说明(备战管理器 · 出装)

装备静态数据统一放在 **`public/equip.json`**(由 Bun 后端静态托管,
前端将来启动时 `fetch("/equip.json")` 加载一次即可)。

> 当前 `equip.json` 是**示例骨架**:按「定位」作为根分类分组,
> 每组内含 3 个层级的示例装备,`icon` 先留空(`""`),等你填入真实 CDN 链接。
> 装备数据填好后告诉我,我再把前端接上(每方案一排固定 6 个出装槽 + 点击槽位弹出选择)。

---

## 数据组织:定位为根分类, tier 为第二分类

> **设计决定**:装备按 **定位 → 层级(tier)** 两级组织。
> 6 个出装槽位 = 一套最终出装(六神装,自由搭配任意定位/层级的装备),
> 选择装备时可按定位分组、在组内按层级(1/2/3)浏览。

顶层 key = 定位(`攻击` / `防御` / `法术` / `通用` …),
第二层 key = 层级字符串 `"1"` / `"2"` / `"3"`,
最内层是该定位 + 该层级的装备数组,每条装备含 **装备名 + 图标 URL**:

```json
{
  "攻击": {
    "1": [ { "name": "锋利短剑", "icon": "" } ],
    "2": [ { "name": "破甲斧",   "icon": "" } ],
    "3": [ { "name": "裂空斩刀", "icon": "" } ]
  },
  "防御": {
    "1": [ { "name": "布甲护腕", "icon": "" } ]
  }
}
```

## 字段说明

| 层级 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| 顶层 key | string | ✅ | 装备定位(根分类), 如 `攻击`/`防御`/`法术`/`通用`,可自行增删 |
| 第二层 key | string | ✅ | 装备层级(tier), 取 `"1"` / `"2"` / `"3"` |
| `name` | string | ✅ | 装备名。**作为方案里的唯一标识**,重名会互相覆盖,请保持唯一 |
| `icon` | string | ✅ | 装备图标完整 URL(远程 CDN),当前留空 `""`,填好后即可显示 |

> 字段名用英文;值为中文不影响。如需额外字段(如描述、合成路径)以后可加,不影响旧数据。

## 保存格式(用户方案, 由 data.json 自动管理, 无需手动编辑)

每个方案的出装存为装备名的数组,基础 6 格;在「修改出装」弹窗里可通过
“增加一格”按钮追加任意格数(用于摆放 1/2 级过渡装),空槽为 `null`。
每格还可有最多 **2 个备用装备**,分别存放在与 `equips` 等长对齐的
`equipsBackup`(备用1)和 `equipsBackup2`(备用2)数组中
(弹窗内右键装备 → “添加备用装备”,再拖入装备):

```json
{
  “name”: “新符文方案”,
  “lane”: “打野”,
  “player”: “2966502203THe梦”,
  “runes”: { “colorful”: [“觉醒·磐固”], “attack”: [], “defense”: [], “general”: [] },
  “equips”: [“锋利短剑”, “破甲斧”, null, null, null, null],
  “equipsBackup”: [null, “裂空斩刀”, null, null, null, null],
  “equipsBackup2”: [null, null, null, null, null, null]
}
```

> `lane`(分路)与 `player`(选手)为方案标签,通过方案卡标题旁的下拉选择;
> 可选项按英雄动态来自 `E:\kaifa\英雄排行\MOBA游戏梯度\曙光英雄\英雄介绍.json`
> 的“常用分路”/“擅长选手”字段(服务端实时读取,重启后刷新页面即可拿到最新数据)。
> 新建方案无需命名(默认名“新方案”);方案卡上的“✎ 备注”按钮可为方案添加备注(即此 `name` 字段,可选)。

> **equip-summary(方案卡片上的出装摘要)展示规则**:直接展示**全部装备**,
> 行数按内容自适应:**底行**为三级主装备紧凑排列;**非三级主装备**浮在其后
> 第一件三级主装备的正上方(多个非三级依次向上堆叠,越早出现的越靠上),
> 末尾没有三级装备的非三级挂到最后一列;**备用1**在三级主装备正上方
> (非三级链之上),**备用2**在其下方。
> 特例:若某三级装备**有备用装备**,则排在其前的非三级装备不再放到它的上方,
> 而是改排到**前一个三级装备的下方**(依次排列)。
> 非三级装备名称带 ·T1/·T2 等级标注,空格不占摘要列。
> 备用装备与主装备在同一格内裁剪拼接展示,不占用额外摘要格:
> 主+1 个备用用对角切割(主=左上半,备=右下半);主+2 个备用用三等分切割
> (主=左三角,备1=中带,备2=右三角)。
> 提示:装备改名会导致旧方案里对应槽位失效(显示为空),所以改名后请同步检查已有方案。
